# Ninguém baixa os CSVs de lead pela URL — só o salvar-lead.php (no
# servidor) lê e escreve aqui. Cobre Apache 2.2 e 2.4.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
